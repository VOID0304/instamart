{
    "version":1,
    "builds":[
        {
            "src":"dist/build.js",
            "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/dist"
        }
    ]
}